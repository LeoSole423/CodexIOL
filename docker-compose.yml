services:
  iol-cli:
    build: .
    container_name: iol-cli
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/Argentina/Buenos_Aires
    volumes:
      - ./data/cli-state:/app/data/cli-state
    stdin_open: true
    tty: true
    command: ["tail", "-f", "/dev/null"]
