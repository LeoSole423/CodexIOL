services:
  iol-cli:
    build: .
    container_name: iol-cli
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/Argentina/Buenos_Aires
    volumes:
      - ./data/cli-state:/app/data/cli-state
      - ./data/review:/app/data/review
      - ./reports/monthly:/app/reports/monthly
      - ./config:/app/config
    stdin_open: true
    tty: true
    command: ["tail", "-f", "/dev/null"]
